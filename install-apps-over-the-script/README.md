# Install Apps over the Script
 a script that can be used to install desired apps and *.deb packages at once, including setting the hostname for the linux instance (adjust the desired values in the script)
