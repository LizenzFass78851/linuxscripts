## Create Hostlist
- This script generates a hosts file listing all subdomains of a specified domain—for example, to block or allow all subdomains of a given domain using firewalls or host-list-based blocking systems. 
- It is useful for legacy firewalls that cannot handle Adblock Plus-style blocklists.
